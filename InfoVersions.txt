v3.3: 

- Test to open create a histogram and open it with javaviwer:
    - Create 2 fits files (image and binary table). After that create histogram over binary table
    fits_make_hist(histptr, fptr, // Binary table and image pointers
                        FLOAT_IMG,
                        naxis, naxes, colnum,
                        amin,  amax, 
                        binSize,
                        FLOATNULLVALUE,
                        0, 0, NULL,
                        &status
                        )
                        ==> Failed
    - change order of pointers ==> Failed

    - No falla ==> fits_make_hist(histptr, fptr,
                        FLOAT_IMG,
                        naxis, naxes, &naxis,
                        &amin_aux,  &amax_aux, 
                        &binSize_aux,
                        FLOATNULLVALUE,
                        0, 0, NULL,
                        &status
                        )

    - histpr doesnt have anything